🔴 CORRECTION CRITIQUE: Incohérence height_m / height_cm

Le backend attend `height_cm` (centimètres) mais le frontend utilise `height_m` (mètres) dans certains endroits. Cela cause des erreurs de validation lors de l'attach de session.

CORRECTIONS REQUISES:

1. Dans `contexts/QuestionnaireContext.tsx` (lignes 39-43):
   Supprimer la conversion height_cm → height_m. Remplacer:
   ```typescript
   const transformedInitialData = initialData ? {
     ...initialData,
     height_m: (initialData as any).height_cm ? (initialData as any).height_cm / 100 : initialData.height_m,
   } : undefined;
   ```
   Par:
   ```typescript
   const transformedInitialData = initialData;
   ```

2. Dans `lib/validation/profileSchema.ts` (ligne 24):
   Changer la validation min de 100 à 50 pour correspondre au backend:
   ```typescript
   height_cm: z.number().int().min(50, 'Height must be at least 50cm').max(250, 'Height must be at most 250cm'),
   ```

3. Dans `lib/validation/profileSchema.ts` (ligne 169, fonction `isQuestionnaireComplete`):
   Changer `'height_m'` en `'height_cm'` dans le tableau `requiredFields`.

4. Rechercher TOUS les usages de `height_m` dans tout le codebase et les remplacer par `height_cm`.

IMPORTANT: Le backend Laravel attend strictement `height_cm` (entier 50-250). Si besoin d'afficher en mètres, faire la conversion uniquement pour l'affichage, jamais pour le stockage/envoi au backend.

Merci de corriger ces problèmes pour assurer la compatibilité avec le backend.

